services:
  sito:
    image: python:3.12-alpine
    container_name: sito-statico
    working_dir: /app
    volumes:
      - ./:/app:ro
    command: python3 -m http.server 8000
    expose:
      - "8000"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true